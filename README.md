# StringList-C-
This project implements an undoable list using the StringList class. The class allows users to manage a list of strings with the ability to undo recent modifications. The implementation follows strict method naming and input/output requirements to ensure compatibility with the provided test program.
